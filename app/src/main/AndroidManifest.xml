<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ms.wmm.client">
        
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/polanokolo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.loginView.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.loginView.RegisterActivity" />
        <activity
            android:name=".activity.groupsListView.GroupsListActivity"
            android:theme="@style/MenuTheme" />
        <activity
            android:name=".activity.groupView.OpenedGroupActivity"
            android:theme="@style/MenuTheme" />
        <activity android:name=".activity.groupsListView.AddGroupActivity" />
        <activity android:name=".activity.groupView.AddUserActivity" />
        <activity android:name=".activity.groupView.AddTransaction" />
        <activity android:name=".activity.groupView.ClosedGroupActivity"></activity>
    </application>

</manifest>